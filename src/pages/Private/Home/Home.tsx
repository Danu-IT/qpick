import { useNavigate } from "react-router-dom";
import Header from "../../../components/Header/Header";

const Home = ({}) => {
  return (
    <div>
      <Header></Header>
    </div>
  );
};

export default Home;
